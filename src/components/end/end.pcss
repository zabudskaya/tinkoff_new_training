.end{

    &__buttons{
        display: flex;
    }
    &__next{
        border-radius: 5px;
        border: 1px solid var(--color__light-grey);
        padding: 10px 15px;
        display: flex;
        align-items: center;
        margin-bottom: 10px;
    }
}